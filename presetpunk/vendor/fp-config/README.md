# @atov/fp-config (vendored)

Generated postcard bindings for the Faderpunk config protocol.

Copied from `configurator/node_modules/@atov/fp-config/` after
`./gen-bindings.sh`. Re-copy when `libfp` protocol types change.
